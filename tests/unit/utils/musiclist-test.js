import musiclist from 'karaoke-web/utils/musiclist';
import { module, test } from 'qunit';

module('Unit | Utility | musiclist', function() {

  // Replace this with your real tests.
  test('it works', function(assert) {
    let result = musiclist();
    assert.ok(result);
  });
});
